class TransitionScreen {
    constructor(game, level, x, y, gameOver) {
        Object.assign(this, { game, level, x, y, gameOver });
        //this.elapsedTime = 0;
    };

     update() {

    };
    draw(ctx) { 
    
        
        
    }
    
};